;;; The following lines added by ql:add-to-init-file:
#-quicklisp
(let ((quicklisp-init #P"~/comp.misc/lisp/quicklisp/setup.lisp"))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))

(ql:quickload "fiveam")
(ql:quickload "trivial-ssh")

;;(require :asdf)
;;(push #P"~/comp.misc/lisp/systems" asdf:*central-registry*)
